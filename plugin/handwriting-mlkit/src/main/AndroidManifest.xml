<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission
        android:name="${mainApplicationId}.permission.IPC"
        tools:ignore="SystemPermissionTypo" />

    <application
        android:icon="@mipmap/ic_launcher_plugin_generic"
        android:label="@string/app_name">
        <activity
            android:name=".ActivationActivity"
            android:exported="true"
            android:permission="${mainApplicationId}.permission.PLUGIN"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="${mainApplicationId}.plugin.ACTIVATE" />
            </intent-filter>
        </activity>

        <service
            android:name=".MainService"
            android:directBootAware="true"
            android:exported="true"
            android:permission="${mainApplicationId}.permission.PLUGIN"
            tools:targetApi="24">
            <intent-filter>
                <action android:name="${mainApplicationId}.plugin.SERVICE" />
            </intent-filter>
        </service>
    </application>
</manifest>
